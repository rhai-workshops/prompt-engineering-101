apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: openwebui
  labels:
    app: openwebui
spec:
  to:
    kind: Service
    name: openwebui
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
